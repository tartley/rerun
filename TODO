* Allow rerun to work on aliases and functions
    * fix call to subprocess
    * fix the tests

* Put TODO into issues
* Press return to re-run anyway
* --ignore should accept globs
* display command exitval if not zero
* print timestamp alongside the command
* print colored command/timestamp/changed files
* --no-clear to stop clearing screen
* Print 'verbose' info output in another color
* preserve colors in command output
